{
  "name": "alist-landing-worker",
  "version": "1.0.0",
  "description": "AList landing page worker for plain file downloads with Turnstile protection",
  "main": "src/worker.js",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run test:node && npm run test:powdet",
    "test:node": "node --test tests/logging.test.mjs tests/no-direct-console.test.mjs tests/worker-observability.test.mjs tests/cache-logging.test.mjs tests/ratelimit-logging.test.mjs tests/unified-check-logging.test.mjs tests/landing-downloader-contract.test.mjs src/worker.ticket-issuance-contract.test.mjs",
    "test:postbuild": "node --test tests/landing-asset-wiring.test.mjs",
    "test:powdet": "cd powdet && go test ./...",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "alist",
    "download"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "altcha-lib": "^1.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "html-minifier-terser": "^7.2.0",
    "wrangler": "^3.0.0"
  }
}
